class FilterHelper {
  /*List<ScanRecord> filterBySearchKeyOnly(List<ScanRecord> records, String searchKey) {
    if (records.isEmpty) return [];
    return records.where((record) => searchKey.isEmpty || _foundBySearchKey(searchKey, record)).toList();
  }*/
}
