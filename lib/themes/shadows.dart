import 'package:flutter/material.dart';

/// Shadows
const SHADOW_1 = BoxShadow(color: Color(0x3F000000), blurRadius: 4, offset: Offset(0, 4));
const SHADOW_2 = BoxShadow(color: Color(0x6615161f), offset: Offset(0, -2), blurRadius: 20);
const GLOW_SHADOW_1 = BoxShadow(color: Color(0x3f7946d1), offset: Offset(0, 6), blurRadius: 25);
